 while (str != '\0')
    {
        printf("%c\n", temp);

        // temp = str[strlen(str) - 1 - i];
        // str[strlen(str) - 1 - i] = str[i];
        // str[i] = temp;
        i++;

    }
    printf("raj %s", str);
